---
# Example ngrok credentials file
# 
# ⚠️  IMPORTANT: ALWAYS ENCRYPT BEFORE COMMITTING! ⚠️
#
# To use this file:
# 1. Copy this file to ngrok_credentials.yml
#    cp ngrok_credentials.yml.example ngrok_credentials.yml
#
# 2. Replace the placeholder with your actual ngrok authtoken
#    vi ngrok_credentials.yml
#
# 3. ENCRYPT IT before committing:
#    ansible-vault encrypt playbooks/roles/ngrok/files/ngrok_credentials.yml
#
# 4. Commit the ENCRYPTED file to the repo
#    git add playbooks/roles/ngrok/files/ngrok_credentials.yml
#    git commit -m "Add ngrok credentials (encrypted)"
#
# 5. Run your playbook (no need to pass ngrok_authtoken as extra-vars)
#    ansible-playbook playbooks/ngrok_setup.yaml --ask-vault-pass
#
# Get your authtoken from: https://dashboard.ngrok.com/get-started/your-authtoken
#
# ⚠️  NEVER commit unencrypted credentials! Always use ansible-vault encrypt first!

authtoken: "your_ngrok_authtoken_here"

