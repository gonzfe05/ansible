galaxy_info:
  role_name: claude
  namespace: gonzfe05
  author: Fernando Gonzalez
  description: Installs Claude Code CLI (Anthropic's official CLI for Claude)

  license: license (GPL-2.0-or-later, MIT, etc)

  min_ansible_version: "2.1"

  galaxy_tags: ["claude", "cli", "ai", "anthropic"]

dependencies:
  - role: node
