---
- name: Test
  hosts: localhost
  roles:
    - role: apt_installs
      vars:
        apt_installs_custom:
          - openssh-client
          - git
          - zsh
          - tmux
          - stow
          - fzf
          - zip
          - vim
          - xclip
          - vim-gtk
          - sudo
          - clang-12
      become: yes
      become_user: root
    - role: users
      vars:
        users:
          - name: aleph
            uid: 1001
            shell: /bin/bash
            comment: "Aleph User"
            groups: "admin,sudo"
            password: ""
      become: yes
      become_user: root
    - role: ssh
      become: yes
      become_user: aleph
    - role: env_setup
      become: yes
      become_user: aleph
    - role: dotfiles
      become: yes
      become_user: aleph
    - role: aws
      become: yes
      become_user: aleph
    - role: repos
      become: yes
      become_user: aleph
    - role: go
      become: yes
      become_user: root
 
